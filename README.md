# Skills 2 — Persona coding skills (private)

Four persona-driven coding skills: `god`, `smoker`, `terry-davis`, and `quantum-computing` (the latter revived from the legacy repo). Each folder is a self-contained agent skill (SKILL.md + scripts/ + evals/).

These were separated from the public `skills` monorepo so the public repo holds only the 15 form skills; persona skills live here.
